#include <stdio.h>

/**
 *print_most_numbers - print numbers from 0 to 9 except 2 and 4
 *
 *Return: Always 0 (Success)
 */

int main(int n)
{
  n=4;
  int l, b;
  for (l = 1; l <= n; l++)
    putchar('#');
  if (l =n)
    putchar('\n');
  for  (b = 1; b <= n; b++)
    putchar('#');
}
