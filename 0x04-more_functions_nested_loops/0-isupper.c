#include "main.h"

/**
 * _isupper - return 1 if the entered character is upper and 0 is lower
 * @c: return 1 if upper
 * Return: Always 0.
 */

int _isupper(int c)
{

if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
