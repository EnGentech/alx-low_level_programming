#include "main.h"

/**
 * main - check the code
 * @c: return 1 if upper
 * Return: Always 0.
 */

int _isupper(int c)
{

char c;
if (c == 'A' && c <= 'Z')
{
return (1);
}
else if (c == 'a' && c <= 'z')
{
return (0);
}
