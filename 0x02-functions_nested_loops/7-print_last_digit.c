#include "main.h"

/**
 * print_last_digit - entry point
 * Return: Always 0 (Success)
 * @i: return last value
 */

int print_last_digit(int i)
{
int val;
val = i % 10;
if (last_digit < 0)
last_digit *= -1;

_putchar(last_digit + '0');

return (last_digit);
return(val);
}
