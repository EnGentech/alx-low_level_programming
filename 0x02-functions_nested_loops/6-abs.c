#include "main.h"

/**
 * _abs - entry point
 * @x: return positive values and ignore the negative sign
 * Return: Always 0 (Success)
 */

int _abs(int x)
{
if (x >= 0)
return (x);
else
return (-x);
}
