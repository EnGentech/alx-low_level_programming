#include <stdio.h>

/**
 * main - Entry point
 * Print only values in units
 *Return: Always 0 (Success)
 */

int main(void)
{
int num;
for (num = 0; num <= 9; num++)
printf("%d", num);
putchar('\n');
return (0);
}
