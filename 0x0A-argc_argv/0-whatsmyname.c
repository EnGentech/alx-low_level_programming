#include <stdio.h>

/**
 * main - return my name
 * @argc: count length
 * @argv: display my name
 * Return: always 0 Success
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
